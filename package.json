{
  "name": "resumate-apk",
  "version": "1.0.0",
  "private": true,
  "description": "ResuMate copyright-safe intelligent resume builder",
  "scripts": {
    "build": "mkdir -p dist && cp -r www/* dist/",
    "android:sync": "npx cap sync android",
    "android:debug": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/cli": "^8.5.1",
    "@capacitor/core": "^8.5.1"
  }
}
